# /src/learning — LEARNING layer

Замыкает цикл DATA → ANALYSIS → KNOWLEDGE → DECISION → AI → UI → LEARNING (CLAUDE.md §3): обрабатывает user decisions и outcomes, обновляет KNOWLEDGE.

Наполняется по мере реализации learning loop (после MVP recommendation flow).
